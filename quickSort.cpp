#include "quicksort.h"
#include <vector>

static void qsHelper(std::vector<int>& list, int low, int high) {

}

static void partition(std::vector<int>& list, int low, int high) {

}

void quickSort(std::vector<int>& list) {
	qsHelper(list, 0, list.size() - 1);
}